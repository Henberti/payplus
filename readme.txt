to run the project please add .env folder 
and add those keys
MONGO_URI -> the mongo uri for connecting to the db
PORT -> port that the app is listening in
JWT_SECRET -> random hash for creating jwt token :
    you can run openssl rand -hex 64 and copy the hash

run npm run dev to start the project
